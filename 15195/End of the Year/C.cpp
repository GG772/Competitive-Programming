#include <bits/stdc++.h>

using namespace std;
using ll = long long;

void solve() {
    vector<int> al, ar, bl, br, cl, cr;
    int x, y, z; cin >> x >> y >> z;

    for (int i = 0; i < x; i++) {
        al.push_back(0);
        br.push_back(0);
    }

    for (int i = 0; i < y; i++) {
        ar.push_back(1);
        cl.push_back(1);
    }

    for (int i = 0; i < z; i++) {
        
    }
}

int main() {
    int t; t = 1;
    while (t--) solve();
}