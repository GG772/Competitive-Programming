// Problem: https://codeforces.com/contest/2072/problem/F


#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL); // Disable cin-cout tie for faster I/O

    int t; cin >> t;

    const int N = 1e6 + 5;

    vector<int> f(N);

    while (t--) {
        int n, k; cin >> n >> k;
        
        
    }
}