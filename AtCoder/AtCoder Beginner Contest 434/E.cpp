#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    int n; cin >> n;
    vector<ll> a(n), b(n);

    vector<int> ind1(n), ind2(n);

    iota(ind1.begin(), int1.end(), 0);
    iota(ind2.begin(), int2.end(), 0);

    for (int i = 0; i < n; i++) {
        cin >> a[i] >> b[i];
    }

    sort(ind1.begin(), ind1.end(), [&](int i, int j) {
        return;
    })
}